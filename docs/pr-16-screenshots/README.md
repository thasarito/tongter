# PR 16 screenshot preview

This is a genuine Chromium/WebGL screenshot of the decorated admin studio, resized to 480 x 320 and compressed as WebP for an inline PR preview. The source PNG is 1440 x 960.

Captured from commit `461fde652696d65c38a5cd0fceae21fe3a0ea4d1` by `tests/e2e/studio-wedding-decor-screenshots.spec.ts` in successful Actions run 35176618503. The fixture is the repository's public reference layout with an empty guest list. The capture intercepts the layout API and rejects mutation requests; it does not read or write live Google Sheets data.

Full-resolution captures (desktop overview, stage, head table, cake table, mobile walkthrough, mobile controls):
https://github.com/thasarito/tongter/actions/runs/35176618503/artifacts/10478901068

The Actions artifact expires on 2026-09-24. The inline WebP preview is retained here independently of that artifact.
